# Pinned to versions actually resolved and verified importable on
# Windows 11 / Python 3.12.10 / Intel Core Ultra (no CUDA).

# --- web ---
fastapi==0.141.1
uvicorn[standard]==0.52.3
python-multipart==0.0.32
pydantic==2.13.4
pydantic-settings==2.15.0

# --- vector / numerics ---
faiss-cpu==1.15.0
numpy==2.5.2
scipy==1.18.0

# --- embeddings (CPU only; torch deliberately absent from the hot path) ---
onnxruntime==1.28.0
tokenizers==0.22.2
transformers==5.15.0          # AutoTokenizer only; warns about missing torch, harmless

# --- data ingestion ---
pyarrow==25.0.1               # read MSMARCO-XI parquet directly; do NOT use `datasets`
huggingface-hub==1.27.0
tqdm==4.70.0

# --- external services ---
google-genai==2.18.1
elevenlabs==2.64.0
httpx==0.28.1

# --- orchestration / observability ---
tenacity==9.1.4
structlog==26.1.0
orjson==3.12.0
python-dotenv==1.2.2
